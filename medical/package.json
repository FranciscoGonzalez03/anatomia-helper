{
  "name": "anatomia-cards-offline",
  "version": "1.0.0",
  "private": true,
  "description": "Flashcards offline de anatomia para mobile con Capacitor",
  "scripts": {
    "serve": "npx serve www",
    "cap:sync": "npx cap sync",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "serve": "^14.2.4"
  }
}